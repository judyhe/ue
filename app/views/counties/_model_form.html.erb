<%= simple_form_for(county, :html => {:class => "default"}) do |f| %>
	
  <%= f.input :name, :input_html => {:class => 'auto'} %>
  <%= f.association :state %>
  <p>
    <%= f.button :submit %>
  </p>
<% end %>