<h1><%= @grades.size %> grades <%= link_to 'new', new_grade_path %></h1>

<ul>
<% @grades.each do |grade| %>
  <li><%= link_to grade.name, edit_grade_path(grade) %></li>
    <!--<td><%= link_to 'Destroy', grade, :confirm => 'Are you sure?', :method => :delete %></td>-->
<% end %>
</ul>


