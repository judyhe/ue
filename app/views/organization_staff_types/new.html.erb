<% title 'New organization staff type' %>

<%= simple_form_for(@organization_staff_type) do |f| %>
  <%= f.input :position, :input_html => {:class => 'auto'} %>
  <p>
    <%= f.button :submit %>
  </p>
<% end %>

<%= link_to 'Back', organization_staff_types_path %>