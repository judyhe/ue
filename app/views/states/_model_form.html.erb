<%= simple_form_for(state, :html => {:class => "default"}) do |f| %>

	<%= f.input :abbr, :label => "Abbreviation", :input_html => {:size => 2, :class => 'auto'} %>
	<%= f.input :name, :input_html => {:class => 'auto'} %>
	
  <p>
    <%= f.button :submit %>
  </p>
<% end %>

